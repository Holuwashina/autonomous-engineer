# CCEO — never commit live resource definitions, run logs, or local Claude overrides
.cceo/resources.yaml
.cceo/resources.*.yaml
!.cceo/resources.yaml.example
.cceo/runs/

.claude/settings.local.json
.mcp.json

# macOS noise
.DS_Store
