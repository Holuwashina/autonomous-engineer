# Autonomous Engineer — never commit live resource definitions, run logs, or local Claude overrides
.ae/resources.yaml
.ae/resources.*.yaml
!.ae/resources.yaml.example
.ae/runs/

# Legacy paths (pre-rename) — kept gitignored so anyone with an older checkout doesn't accidentally commit them
.cceo/resources.yaml
.cceo/resources.*.yaml
.cceo/runs/

.claude/settings.local.json
.mcp.json

# macOS noise
.DS_Store
